version: v2

managed:
  enabled: true

plugins:
  - local: protoc-gen-go
    out: ..
    opt: paths=source_relative
  - local: [ "sh", "-c", "cd ../codesjoy/reason && [ -f go.mod ] || go mod init github.com/codesjoy/pkg/proto/codesjoy/reason && go mod tidy" ]
    out: ..
